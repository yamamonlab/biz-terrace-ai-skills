# Reference examples

61枚等の見本画像は、このSkillパッケージには含めない。

利用時は pattern ID ごとに参照できる形で配置することを推奨する。

例：
- `examples/pattern-07/01.png`
- `examples/pattern-17/01.png`
- `examples/pattern-45/01.png`

重要：見本は Visual Router で型が確定した後にのみ参照する。数値と文言を見本から転記しない。借りるのは構図・配置・線種・強調の置き方だけ。
